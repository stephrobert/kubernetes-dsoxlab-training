id: ckad-init-container
title: "Wait for a dependency with an init container"
level: application-design
description: |
  Keep an application from starting before the Service it depends on answers,
  with an init container that waits. Watch the Pod held in Init, put the
  dependency in place, and see the application start on its own.
skills:
  - init-container
  - pod-design
  - startup-dependency
  - services
distros:
  - ubuntu24
doc_url: https://blog.stephane-robert.info/docs/conteneurs/orchestrateurs/kubernetes/init-containers-sidecars/
lab_type: lab
estimated_time: 15m

# Compétence du blueprint CKAD, domaine Application Design and Build (20 %) :
# « Understand multi-container Pod design patterns (init containers) ».
certification_tags:
  - ckad

runtime:
  type: vm
  targets:
    - name: cp
      host: k8s-cp.lab
  default: cp

validation:
  functional: true
  persistence_after_reboot: false
