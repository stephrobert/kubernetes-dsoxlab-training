---
# Joué par `dsoxlab clean` : défait ce que le lab a créé. Role et RoleBinding
# vivent dans le namespace lab, le supprimer suffit. Le cluster reste.
- name: Nettoyer le lab ckad-rbac-role-rolebinding
  hosts: lab_target
  become: true
  tasks:
    - name: Supprimer le namespace de travail
      ansible.builtin.command: >-
        kubectl --kubeconfig /etc/kubernetes/admin.conf delete namespace lab
        --ignore-not-found --wait=false
      changed_when: true
      failed_when: false
