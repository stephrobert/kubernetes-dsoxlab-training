---
# Joué par `dsoxlab run` avant d'ouvrir la session.
- name: Préparer le lab cka-storageclass-provisionnement-dynamique
  hosts: lab_target
  become: true
  tasks:
    - name: Installer le socle, le cluster kubeadm du catalogue
      ansible.builtin.include_tasks: ../../shared/kubeadm-cluster.yml

    - name: Installer le provisionneur de stockage local
      # local-path-provisioner de Rancher, v0.0.33, mesuré le 2026-09-17 :
      # il crée la StorageClass `local-path`, en WaitForFirstConsumer et
      # reclaimPolicy Delete. C'est le provisionneur que tout cluster kubeadm
      # sans fournisseur de cloud emploie, kind compris.
      ansible.builtin.command:
        cmd: >-
          kubectl --kubeconfig /etc/kubernetes/admin.conf apply -f
          https://raw.githubusercontent.com/rancher/local-path-provisioner/v0.0.33/deploy/local-path-storage.yaml
      changed_when: true

    - name: Attendre que le provisionneur réponde
      ansible.builtin.command:
        cmd: >-
          kubectl --kubeconfig /etc/kubernetes/admin.conf -n local-path-storage
          rollout status deployment/local-path-provisioner --timeout=300s
      changed_when: false

    - name: Poser la situation du lab
      ansible.builtin.script: fixtures/prepare.sh
      args:
        executable: /bin/bash
      environment:
        KUBECONFIG: /etc/kubernetes/admin.conf
